1. Install packages Using "npm install". It fetches all the dependencies into node_modules folder.
2. To Run this application in your localhost use "npm run start" command.
3. To create a bundle which will be deployed  use "npm run deploy" command.